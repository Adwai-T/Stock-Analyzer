package in.adwait; // Package declaration for the Java code. This helps organize classes.

import in.adwait.matrix.Matrix;
import org.jocl.*; // Imports all classes from the org.jocl package, which provides Java bindings for OpenCL.
import java.io.InputStream; // Used for reading the kernel source file from resources.
import java.nio.charset.StandardCharsets; // Used to specify character encoding when reading the kernel file.
import java.util.Scanner; // Used to read the content of the kernel file.

import static org.jocl.CL.*;

public class Main {
    public static void main(String[] args) {

    }
}
